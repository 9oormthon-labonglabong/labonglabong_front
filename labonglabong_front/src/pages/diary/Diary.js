import React from "react";

import styled from "styled-components";

import { format } from "date-fns";

const Diary = () => {
  return <Layout>다이어리입니다.</Layout>;
};

export default Diary;

const Layout = styled.section`
  margin: 20px;
`;

const Header = styled.div``;
